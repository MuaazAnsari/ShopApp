<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>

        <main class="centered">
            <h1> <%= product.title %></h1>
            <hr>
            <div>
                <img src="<%= product.imageUrl %>" alt="<%= product.title %>">
            </div>
            <h2>$<%= product.price %></h2>
            <p> <%= product.description %></p>

            <!-- Since the include file dont have access to product object, we need to pass a new object 
            as argument which takes a New key which is used to access it in include file. -->
            <%- include('../includes/add-to-cart.ejs', {product : product}) %>
            
        </main>

<%- include('../includes/end.ejs') %>